# Sample output status

No extracted facts or relationship demonstrations exist in Milestone 1.
The authored PDF under `tests/fixtures` is a conversion test, not a sample AI
result. `docling-smoke/` contains actual conversion artifacts from that PDF,
including structured Docling output, validated block locators and a run report.
OCR and table inference were disabled for this prose-only smoke test. These
artifacts do not establish performance on starter PDFs. Versioned fact and
relationship exports will be added only after actual processing.
